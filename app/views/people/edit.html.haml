.panel
  = form_for @person do |f|
    .panel-title
      %h1 Editing Person
      .l-menu= link_to "Cancel", person_path(@person)
      .r-menu
        = f.submit 'Done'
    .panel-body.scrollable.pad
      = render partial: 'form', locals: {f: f}
