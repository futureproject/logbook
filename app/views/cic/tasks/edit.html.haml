= form_for [:cic, @task], html: { class: 'list-detail panel' } do |f|
  .panel-title
    %h1 Editing Task
    .l-menu= link_to "Cancel", cic_tasks_path
    .r-menu
      = f.submit 'Done'
  .panel-body.scrollable.pad
    = render partial: 'form', locals: {f: f}
    .field
      = link_to "Delete Task", cic_task_path(@task), class: 'warning button', data: {method: 'delete', confirm: 'Are you sure?' }

