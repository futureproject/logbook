.reflection
  .reflection-date= link_to reflection.created_at.strftime("%D"), reflection_path(reflection)
  .reflection-content
    :markdown
      #{reflection.content}
  .mini-assets
    - reflection.assets.each do |a|
      =link_to image_tag(a.thumbnail), a.data.url(:original), class: 'mini-asset'

