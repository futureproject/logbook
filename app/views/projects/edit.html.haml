%h1 Editing Project

= render 'form'

%hr
%p=link_to 'Delete Project', project_path(@project), data: {method: 'delete', confirm: 'Are you sure you want to delete this project?'}, class: 'warning button'
%hr
%p= link_to 'Cancel', project_path(@project)
