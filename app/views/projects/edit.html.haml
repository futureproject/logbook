#projects_list.list

.panel.list-detail
  = form_for @project do |f|
    .panel-title
      %h1 Editing Project
      .l-menu= link_to "Cancel", project_path(@project)
      .r-menu
        = f.submit 'Done'
    .panel-body.scrollable.pad
      = render partial: 'form', locals: {f: f}
