.show-record
  %header
    %h1 Editing Project

  = render 'form'

  =link_to "Delete Project", logbook_project_path(@project), class: 'delete', data: { method: 'delete', confirm: "Are you sure you want to delete this project?"}
