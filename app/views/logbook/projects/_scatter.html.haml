:javascript
  $(document).ready(function(){
    $("#{container}").highcharts({
      credits: false,
      colors: #{colors},
      chart: {
        type: 'scatter',
        zoomType: 'xy',
        backgroundColor: "#f6f7f6",
      },
      title: false,
      xAxis: {
        title: {
          text: 'Leaders (count)'
        }
      },
      yAxis: {
        title: {
          text: 'Supporters (count)'
        }
      },
      legend: {
        layout: 'horizontal',
        verticalAlign: 'bottom',
      },
      plotOptions: {
        bubble: {
          tooltip: {
            headerFormat: "{series.name}<br>",
            pointFormat: "<b>{point.title}</b><br>{point.y} Hrs, {point.z} Attendees" +
              "<br>{point.notes}"
          },
          events: {
            click: function(event) {
              path = "/logbook/projects/" + event.point.id
              path += "?scope_id=" + ds.CONSTANTS.scope.id + "&scope_type=" + ds.CONSTANTS.scope.type
              location = path
            }
          }
        }
      },
      series: #{JSON.dump(data)}
    })
  })
