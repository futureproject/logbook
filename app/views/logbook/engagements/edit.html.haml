.show-record
  %header
    %h1 Editing Engagement
  .button-actions
    =link_to "Cancel", logbook_engagement_path(@engagement), class: 'cancel button'
    =link_to "Destroy", logbook_engagement_path(@engagement), class: 'delete button', data: { method: 'delete', confirm: "Are you sure?"}

  = render 'form'
