.show-record
  %header
    %h1 Editing Person

  = render 'form'

  =link_to "Delete Person", logbook_person_path(@person), class: 'delete', data: { method: 'delete', confirm: "Are you sure you want to delet this person?"}
